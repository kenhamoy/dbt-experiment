fixed:
  type: bigquery
  method: service-account
  project: ae-experiments # Replace this with your project id
  dataset: postgres_sample_data_demo # Replace this with dbt_your_name, e.g. dbt_bob
  threads: 4
  timeout_seconds: 300
  location: US
  priority: interactive
  keyfile: "{{ env_var('BIGQUERY_KEYFILE') }}"